<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <!-- Settings Form View for Gandola Manager -->
        <record id="gandola_manager_settings_view_form" model="ir.ui.view">
            <field name="name">gandola.manager.settings.form</field>
            <field name="model">res.config.settings</field>
            <field name="priority" eval="40"/>
            <field name="arch" type="xml">
                <form string="Gandola Settings" class="o_account_settings">
                    <app data-string="Gandola Manager">
                        
                        <!-- Section for Invoicing Defaults -->
                        <h2>Invoicing Defaults</h2>
                        <div class="row mt16 o_settings_container">
                             <!-- Setting for Default Products -->
                             <div class="col-12 col-lg-6 o_setting_box">
                                <div class="o_setting_left_pane"/>
                                <div class="o_setting_right_pane">
                                    <label for="gandola_product_ids"/>
                                    <div class="text-muted">
                                        Select the products that will be automatically added to new invoices.
                                    </div>
                                    <div class="content-group">
                                        <div class="mt16">
                                           <!-- The 'many2many_tags' widget is ideal for this field -->
                                           <field name="gandola_product_ids" widget="many2many_tags" />
                                        </div>
                                    </div>
                                </div>
                            </div>
                             <!-- Setting for Default Payment Term -->
                             <div class="col-12 col-lg-6 o_setting_box">
                                <div class="o_setting_left_pane"/>
                                <div class="o_setting_right_pane">
                                    <label for="gandola_payment_term_id"/>
                                    <div class="text-muted">
                                       Set the default payment term for invoices created from a site.
                                    </div>
                                    <div class="content-group">
                                        <div class="mt16">
                                           <field name="gandola_payment_term_id" />
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                    </app>
                </form>
            </field>
        </record>

        <!-- Action to open the Settings screen -->
        <record id="gandola_manager_settings_action" model="ir.actions.act_window">
            <field name="name">Settings</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">res.config.settings</field>
            <field name="view_mode">form</field>
            <field name="target">inline</field>
            <field name="context">{'module' : 'gandola_manager', 'bin_size': False}</field>
        </record>

        <!-- Menu item to access the Settings -->
        <!-- This assumes you have a root menu with id="gandola_manager_menu_root" -->
        <menuitem
            id="gandola_manager_settings_menu"
            name="Settings"
            parent="gandola_manager.menu_root" 
            sequence="0"
            action="gandola_manager_settings_action"
            groups="base.group_system"/>

    </data>
</odoo>

